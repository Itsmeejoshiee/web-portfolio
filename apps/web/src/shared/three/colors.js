// Literal hex mirrors of shared/styles/tokens.css — three.js materials can't
// read CSS custom properties, so the sticker layer needs its own copies.
// Keep these in sync with tokens.css if the palette ever changes.
export const THREE_COLORS = {
  ink: '#2C2418',
  bg: '#FAF6EF',
  accent: '#D85A30',
  peachBg: '#F5C4B3',
  lavenderBg: '#CECBF6',
  mintBg: '#9FE1CB',
  periwinkle: '#7F77DD',
  green: '#5DCAA5',
};
